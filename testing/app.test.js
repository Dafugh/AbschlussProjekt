const app = require('../app');

test('Test CreateID for 6 digits', () => {
    expect(app.createId.toString().length).toBe(5);
});
test('Test CreateID for 6 digits', () => {
    expect(app.createId.toString().length).toBe(5);
});